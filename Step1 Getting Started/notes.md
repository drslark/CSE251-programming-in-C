```shell
# This command makes it easier to tell directories from files.
~ >>> ls -F
playground/
```

```shell
# This command starts a gui that makes it easier to view available help.
~ >>> yelp &
```
